apiVersion: v1
kind: ConfigMap
metadata:
  name: catalog-config
  namespace: cafe-boreal
data:
  DB_HOST: "postgres-service.cafe-boreal.svc.cluster.local"
  DB_PORT: "5432"
  DB_NAME: "cafeboreal"
  DB_USER: "postgres"
  PORT: "3001"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-api
  namespace: cafe-boreal
spec:
  replicas: 2
  selector:
    matchLabels:
      app: catalog-api
  template:
    metadata:
      labels:
        app: catalog-api
    spec:
      containers:
      - name: catalog-api
        image: catalog-api:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3001
        env:
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: catalog-config
              key: DB_HOST
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: catalog-config
              key: DB_PORT
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: catalog-config
              key: DB_NAME
        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: catalog-config
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: password
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: catalog-config
              key: PORT
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: cafe-boreal
spec:
  selector:
    app: catalog-api
  ports:
  - port: 3001
    targetPort: 3001
  type: ClusterIP
